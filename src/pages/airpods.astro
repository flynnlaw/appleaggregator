---
import Layout from '../layouts/Layout.astro';
import ProductPanel from '../components/ProductPanel.astro';
import laptopsData from '../data/data.json';


interface PaneItem {
  href: string;
  svgPath: string;
  text: string;
  customClass: string;
}

const selectedConfigurations = [
  { config: laptopsData.hardware.airpods[0].configurations[0], model: laptopsData.hardware.airpods[0] },
  { config: laptopsData.hardware.airpods[1].configurations[0], model: laptopsData.hardware.airpods[1] },
  { config: laptopsData.hardware.airpods[2].configurations[0], model: laptopsData.hardware.airpods[2] }
];


const paneItems: PaneItem[] = [
  {
    href: '',
    svgPath: '/icons/airpods_4_icon.svg',
    text: 'Airpods 4',
    customClass: 'iphone-16-style',
  },
  {
    href: '',
    svgPath: '/icons/airpods_pro_icon.svg',
    text: 'Airpods Pro 2',
    customClass: 'iphone-16-style',
  },
  {
    href: '',
    svgPath: '/icons/airpods_max_icon.svg',
    text: 'Airpods Max',
    customClass: 'iphone-16-style',
  },
];


---
<Layout title="Welcome to Astro." paneItems={paneItems}>
  <main>
	  <ul role="list" class="link-card-grid">
      {selectedConfigurations.map(({ config, model }, index) => (
        <li>
          <ProductPanel
            imageUrl={model.imageUrl}
            specs={config.specs}
            pricing={config.pricing}
          />
        </li>
      ))}
    </ul>
  </main>
</Layout>

<style>
  main {
    margin: auto;
    padding: 1rem;
    width: 100%;
    max-width: 1200px;
    color: white;
    font-size: 20px;
    line-height: 1.6;
  }
  .link-card-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }
</style>